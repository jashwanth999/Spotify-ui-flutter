import 'package:flutter/material.dart';

class recentlist extends StatefulWidget {
  @override
  _recentlistState createState() => _recentlistState();
}

class _recentlistState extends State<recentlist> {
  @override
  Widget build(BuildContext context) {
    return Container(width: 120, margin: EdgeInsets.all(4.0), decoration: BoxDecoration(color: Colors.grey, borderRadius: BorderRadius.circular(5.0)));
  }
}
